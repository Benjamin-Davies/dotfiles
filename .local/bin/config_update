#!/bin/sh
set -e
alias config='git --git-dir=$HOME/dotfiles.git/ --work-tree=$HOME'

config pull
config submodule update --init --remote --depth=1

echo "Updating Vim plugins" \
  | nvim -c PlugUpdate -c ':qa!' -
